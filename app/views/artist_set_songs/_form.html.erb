<%= form_with(model: artist_set_song) do |form| %>
  <% if artist_set_song.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artist_set_song.errors.count, "error") %> prohibited this artist_set_song from being saved:</h2>

      <ul>
        <% artist_set_song.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :artist_set_id, style: "display: block" %>
    <%= form.text_field :artist_set_id %>
  </div>

  <div>
    <%= form.label :song_id, style: "display: block" %>
    <%= form.text_field :song_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
