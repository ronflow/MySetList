<%= form_with(model: [@artist, artist_set], local: true) do |form| %>
  <% if artist_set.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artist_set.errors.count, "error") %> prohibited this artist_set from being saved:</h2>

      <ul>
        <% artist_set.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :set_list_name, "Nome do Set:", style: "display: block" %>
    <%= form.text_field :set_list_name, style: "width: 100%; padding: 8px; margin-top: 5px;" %>
  </div>

  <div class="actions" style="margin-top: 15px;">
    <%= form.submit "Salvar Set", 
        style: "background-color: #28a745; color: white; padding: 10px 20px; border: none; border-radius: 4px; cursor: pointer;" %>
  </div>
<% end %>


